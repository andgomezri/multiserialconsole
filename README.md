# multiserialconsole
A multi-console serial terminal in Python for Windows. Run up to 4 parallel COM ports with live ASCII/HEX view toggles, smart string/hex transmission (e.g., 0xFF), ANSI color parsing, dynamic responsive layouts (mixed, vertical, horizontal), and an always-on-top toggle. Clean, lightweight, and thread-safe.
